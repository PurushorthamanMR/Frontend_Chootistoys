# Used automatically by `vite build` (npm run build / npm run deploy).
# www.chootistoys.com has no working reverse proxy for /api, so the frontend
# must call the backend's own domain directly - must match BACKEND_URL in
# Backend/.env on the server.
VITE_API_BASE_URL=https://backend.chootistoys.com/api